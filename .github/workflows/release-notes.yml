
name: release-notes
on:
  push:
    tags:
      - 'v*'
jobs:
  notes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      release: write
    steps:
      - uses: actions/checkout@v4
      - name: Draft release notes
        run: echo "Call Release Notes Agent..."
